<context_dump>

<file path="src/app/layout.tsx">
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IPJNU - Jengkeng",
  description: "Jamiyyah Jengkeng Indonesia",
  manifest: "/manifest.json",
  appleWebApp: {
    capable: true,
    statusBarStyle: "black-translucent",
    title: "JENGKENG",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="id" // 1. Ubah dari "en" ke "id" (Indonesia)
      translate="no" // 2. Atribut sakti penangkal Google Translate
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
</file>

<file path="src/app/manifest.json">
{
  "name": "IPJNU - Jengkeng",
  "short_name": "JENGKENG",
  "description": "Score tracker buat tongkrongan",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#09090b",
  "theme_color": "#09090b",
  "icons": [
    {
      "src": "/icon.svg",
      "sizes": "any",
      "type": "image/svg+xml"
    },
    {
      "src": "/web-app-manifest-192x192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "maskable"
    },
    {
      "src": "/web-app-manifest-512x512.jpg",
      "sizes": "512x512",
      "type": "image/jpeg",
      "purpose": "maskable"
    }
  ]
}
</file>

<file path="src/app/page.tsx">
"use client";

import { useGameStore } from "@/store/useGameStore";
import SetupScreen from "@/components/SetupScreen";
import DashboardScreen from "@/components/DashboardScreen";
import FinishedScreen from "@/components/FinishedScreen";
import InstallPrompt from "@/components/InstallPrompt";
import { useEffect, useState } from "react";

export default function Home() {
  const { gameStatus } = useGameStore();
  const [mounted, setMounted] = useState(false);

  // Avoid hydration errors with persist middleware
  useEffect(() => {
    setMounted(true);
  }, []);

  if (!mounted) return null;

  return (
    <>
      {gameStatus === "idle" && <SetupScreen />}
      {gameStatus === "active" && <DashboardScreen />}
      {gameStatus === "finished" && <FinishedScreen />}

      {/* Pop Up akan nangkring otomatis di bawah */}
      <InstallPrompt />
    </>
  );
}</file>

<file path="src/app/globals.css">
@import "tailwindcss";

@theme {
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

@layer base {
  body {
    @apply bg-zinc-950 text-zinc-100 antialiased selection:bg-amber-500/30;
    /* Hide scrollbar for mobile clean look */
    -ms-overflow-style: none; /* IE and Edge */
    scrollbar-width: none; /* Firefox */
  }

  body::-webkit-scrollbar {
    display: none; /* Chrome, Safari and Opera */
  }
}

@media print {
  @page {
    /* "auto" membuat printer/browser menyesuaikan panjang kertas dengan konten */
    size: auto;
    margin: 0;
  }

  body {
    /* Memastikan body tidak terpotong margin default browser */
    margin: 0 !important;
    padding: 0 !important;
    background: white !important;
    color: black !important;
    font-family: monospace;
  }

  /* Sembunyikan semua tombol dan elemen yang cuma buat layar HP */
  .no-print {
    display: none !important;
  }

  /* Styling tabel biar hemat tinta dan rapi */
  table {
    width: 100%;
    border-collapse: collapse;
  }

  td,
  th {
    padding: 5px;
    border: 1px solid #000 !important;
  }
}
</file>
</context_dump>
